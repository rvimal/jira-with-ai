Review the Jira ticket details and decide what Jira field updates are needed.

Return only JSON with an `update` object that can be sent back through the MCP Jira update tool.

Do not include markdown, prose outside JSON, or unsupported keys.